# Assets

Import these files directly in your components:

```js
import logo from '@/assets/images/logo.png';
```

## Folders

- `images/` — logo, hero images, product photos
- `icons/` — custom SVG icons
